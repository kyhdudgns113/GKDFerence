import {Dispatch, SetStateAction} from 'react'
import {get} from '../../../server'

export const useSetTargetUserOId = (tUOId: string, setTUOId: Dispatch<SetStateAction<string>>) => {
  /**
   * 1. get 함수로 타겟 유저의 ID 를 받아온다.
   */

  return 1
}
